Utilizing a nested for loop, rectangles are drawn to the screen both in rows and 
columns. Each rectangle has a greyscale value that is relative to the pixels 
distance from the center of the window. 